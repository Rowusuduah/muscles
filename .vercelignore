# Ship only the static PWA to Vercel — exclude the Vite/Workers build system,
# the OpenAI-hosting config, and the handbook-generation tooling. These stay in
# the repo (for local dev, tests, and regenerating the handbook) but are not
# needed to serve the site. Mirrors how MoneyTrack deploys: pure static root.
node_modules
dist
.wrangler
.sites-build
.handbook_qa
test
tools
build
worker
.openai
.github
vite.config.mjs
package.json
package-lock.json
*.py
*.docx
GYM
